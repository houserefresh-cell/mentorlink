import { authenticateMeetingUser } from "@/lib/meeting-auth";
import { createSupabaseAdmin } from "@/lib/supabase-admin";
const uuid=/^[0-9a-f-]{36}$/i;
export async function GET(request:Request){const user=await authenticateMeetingUser(request.headers.get("authorization"));if(!user)return Response.json({error:"נדרשת התחברות."},{status:401});if(user.role!=="parent")return Response.json({error:"הגישה מיועדת להורים בלבד."},{status:403});const admin=createSupabaseAdmin();const registrations=await admin.from("mentor_activity_registrations").select("activity_id").eq("parent_user_id",user.id).in("status",["registered","waitlisted","cancelled"]);if(registrations.error)return Response.json({error:"לא ניתן לטעון עדכונים."},{status:500});const ids=[...new Set((registrations.data??[]).map(row=>row.activity_id))];if(!ids.length)return Response.json({updates:[]});const [updates,responses,activities,reads]=await Promise.all([admin.from("mentor_activity_updates").select("id, activity_id, recipient_scope, recipient_parent_user_id, update_type, body, delay_minutes, proposed_start_at, proposed_end_at, created_at").in("activity_id",ids).or(`recipient_scope.eq.all_active,recipient_parent_user_id.eq.${user.id}`).order("created_at",{ascending:false}),admin.from("mentor_activity_postponement_responses").select("update_id, response, responded_at").eq("parent_user_id",user.id),admin.from("mentor_activities").select("id, title").in("id",ids),admin.from("mentor_activity_update_reads").select("update_id, read_at").eq("parent_user_id",user.id)]);if(updates.error||responses.error||activities.error||reads.error)return Response.json({error:"לא ניתן לטעון עדכונים."},{status:500});return Response.json({updates:(updates.data??[]).map(update=>({...update,activityTitle:(activities.data??[]).find(activity=>activity.id===update.activity_id)?.title??"פעילות",response:(responses.data??[]).find(response=>response.update_id===update.id)??null,readAt:(reads.data??[]).find(read=>read.update_id===update.id)?.read_at??null}))},{headers:{"Cache-Control":"no-store"}})}
export async function POST(request:Request){const user=await authenticateMeetingUser(request.headers.get("authorization"));if(!user)return Response.json({error:"נדרשת התחברות."},{status:401});if(user.role!=="parent")return Response.json({error:"הגישה מיועדת להורים בלבד."},{status:403});let payload:Record<string,unknown>;try{payload=await request.json()}catch{return Response.json({error:"בקשה לא תקינה."},{status:400})}const updateId=typeof payload.updateId==="string"?payload.updateId:"";if(!uuid.test(updateId))return Response.json({error:"עדכון לא תקין."},{status:400});const admin=createSupabaseAdmin();if(payload.action==="mark_read"){const visible=await admin.from("mentor_activity_updates").select("id, activity_id").eq("id",updateId).maybeSingle();if(!visible.data)return Response.json({error:"העדכון לא נמצא."},{status:404});const registration=await admin.from("mentor_activity_registrations").select("id").eq("activity_id",visible.data.activity_id).eq("parent_user_id",user.id).maybeSingle();if(!registration.data)return Response.json({error:"אין הרשאה לעדכון."},{status:403});const result=await admin.from("mentor_activity_update_reads").upsert({update_id:updateId,parent_user_id:user.id,read_at:new Date().toISOString()});if(result.error)return Response.json({error:"לא ניתן לסמן את העדכון כנקרא."},{status:422});return Response.json({ok:true})}const response=payload.response;if(!['accepted','declined'].includes(String(response)))return Response.json({error:"תגובה לא תקינה."},{status:400});const result=await admin.rpc("respond_to_activity_postponement",{p_update_id:updateId,p_parent_user_id:user.id,p_response:response});if(result.error)return Response.json({error:"לא ניתן לשמור את התגובה."},{status:422});return Response.json({ok:true})}
