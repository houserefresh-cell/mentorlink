"use client";
import {useEffect,useState}from"react";import{supabase}from"@/lib/supabase";
type Update={id:string;activityTitle:string;update_type:string;body:string;delay_minutes:number|null;proposed_start_at:string|null;proposed_end_at:string|null;created_at:string;response:{response:string}|null};
const labels:Record<string,string>={operational:"עדכון תפעולי",reminder:"תזכורת",equipment:"ציוד",meeting_point:"נקודת מפגש",delay:"עיכוב",postponement:"הצעת דחייה",cancellation:"ביטול",general:"הודעה"};
export default function ParentActivityUpdates(){const[updates,setUpdates]=useState<Update[]>([]),[message,setMessage]=useState("");async function token(){return(await supabase.auth.getSession()).data.session?.access_token??""}async function load(){const response=await fetch("/api/parent/activity-updates",{headers:{Authorization:`Bearer ${await token()}`},cache:"no-store"});if(response.ok)setUpdates((await response.json()).updates??[])}useEffect(()=>{void load()},[]);async function respond(updateId:string,responseValue:string){const response=await fetch("/api/parent/activity-updates",{method:"POST",headers:{Authorization:`Bearer ${await token()}`,"Content-Type":"application/json"},body:JSON.stringify({updateId,response:responseValue})});setMessage(response.ok?"התגובה נשמרה ונשלחה לחונך.":"לא ניתן לשמור את התגובה.");if(response.ok)await load()}if(!updates.length)return null;return <section className="mt-8 rounded-3xl border border-amber-200 bg-amber-50/60 p-6"><div className="flex items-center justify-between"><div><p className="font-black text-amber-800">חשוב לדעת</p><h2 className="text-2xl font-black">עדכונים מהחונכים</h2></div><span className="rounded-full bg-amber-500 px-3 py-1 font-black text-white">{updates.length}</span></div>{message&&<p role="status" className="mt-4 rounded-xl bg-white p-3 font-bold">{message}</p>}<div className="mt-5 grid gap-4">{updates.map(update=><article key={update.id} className="rounded-2xl border bg-white p-5"><div className="flex flex-wrap items-center justify-between gap-2"><div><span className="rounded-full bg-violet-100 px-3 py-1 text-sm font-black text-violet-800">{labels[update.update_type]??"עדכון"}</span><h3 className="mt-2 text-lg font-black">{update.activityTitle}</h3></div><time className="text-sm text-slate-500">{new Intl.DateTimeFormat("he-IL",{day:"numeric",month:"long",hour:"2-digit",minute:"2-digit"}).format(new Date(update.created_at))}</time></div><p className="mt-3 leading-7">{update.body}</p>{update.delay_minutes&&<p className="mt-2 font-black text-amber-800">עיכוב משוער: {update.delay_minutes} דקות</p>}{update.update_type==="postponement"&&update.proposed_start_at&&<div className="mt-4 rounded-xl bg-blue-50 p-4"><p className="font-black">המועד החלופי המוצע</p><p>{new Intl.DateTimeFormat("he-IL",{weekday:"long",day:"numeric",month:"long",hour:"2-digit",minute:"2-digit"}).format(new Date(update.proposed_start_at))}</p>{update.response?<p className="mt-2 font-bold text-blue-800">תגובתכם: {update.response.response==="accepted"?"המועד מתאים":"המועד אינו מתאים"}</p>:<div className="mt-3 flex gap-3"><button onClick={()=>respond(update.id,"accepted")} className="rounded-xl bg-green-600 px-4 py-2 font-black text-white">המועד מתאים</button><button onClick={()=>respond(update.id,"declined")} className="rounded-xl border border-red-300 px-4 py-2 font-black text-red-700">המועד אינו מתאים</button></div>}</div>}</article>)}</div></section>}
